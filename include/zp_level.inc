#if defined _zp_level_included
  #endinput
#endif
#define _zp_level_included

native zp_level_get(id);

native zp_level_get_exp(id);

native zp_level_get_req_exp(id);

native zp_level_add_exp(id, exp);